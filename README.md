# live-code-catcher

> 운빨존많겜 유튜브 라이브 시청자 참여(시참) 초대 코드 자동 입력 프로그램

유튜브 라이브 방송 화면에 송출되는 초대 코드를 자동으로 인식해 운빨존많겜에 입력해주는 자동화 도구입니다.

---

## 설치

```bash
pip install -r requirements.txt
```

---

## 처음 사용 시 설정 (최초 1회)

### 1단계 — 초대 코드 위치 등록 (F2)

1. 유튜브 라이브에서 초대 코드가 화면에 표시된 상태 만들기
2. 마우스를 **초대 코드 숫자 위에 올리고** `F2` 누르기
3. 박스 크기를 자동 감지해 `config.json`에 좌표 저장

### 2단계 — 입력창 위치 등록 (F3)

1. 게임에서 코드 입력창이 열린 상태 만들기
2. 마우스를 **입력창 위에 올리고** `F3` 누르기

### 3단계 — 참가 버튼 위치 등록 (F4)

1. 게임에서 참가하기 버튼이 보이는 상태 만들기
2. 마우스를 **참가 버튼 위에 올리고** `F4` 누르기

> 창 크기나 위치가 바뀌면 해당 키를 다시 눌러 재등록

---

## 실전 사용

```bash
python auto_number.py
```

초대 코드가 뜨기 전에 **F9**를 눌러두면 코드가 감지되는 순간 자동으로 입력 → 참가 완료 후 프로그램이 종료됩니다.  
`F9`를 다시 누르면 중지됩니다.

---

## 단축키 정리

| 키 | 기능 |
|----|------|
| `F2` | 초대 코드 위치 등록 (숫자 위에 마우스 올리고 누르기) |
| `F3` | 입력창 위치 등록 (입력창 위에 마우스 올리고 누르기) |
| `F4` | 참가 버튼 위치 등록 (버튼 위에 마우스 올리고 누르기) |
| `F9` | 자동 대기 시작 / 중지 |
| `ESC` | 프로그램 종료 |

---

## 파일 구성

```
live-code-catcher/
├── auto_number.py   # 메인 프로그램
├── requirements.txt # 패키지 목록
└── config.json      # 등록된 좌표 (자동 생성, gitignore)
```

---

## 주의사항

- 창 크기나 위치가 바뀌면 **F2 / F3 / F4 재등록** 필요
- `debug/raw.png` — OCR 캡처 확인용 (자동 생성)

---

## 키워드

운빨존많겜 시참 자동입력 | 유튜브 라이브 초대코드 자동화 | 시청자참여 매크로 | 운빨존많겜 초대코드 | Python 게임 자동화
